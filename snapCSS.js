(function($) {

  // Create plugin
  $.fn.snapCSS = function() {

    var $html = $('html'),
      $platform,
      $device,
      $version;


    return $html.addClass($platform $device $version);

  }

})(jQuery);
